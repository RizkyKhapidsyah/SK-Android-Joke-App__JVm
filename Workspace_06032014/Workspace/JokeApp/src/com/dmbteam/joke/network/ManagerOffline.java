package com.dmbteam.joke.network;

import android.content.Context;

/**
 * This class is responsible for all operations which occur when the device is
 * not connected to a network
 * 
 * @author |dmb TEAM|
 * 
 */
public class ManagerOffline {

    /**
     * Loads the error activity in given context
     * @param context
     */
	public static void loadErrorActivity(Context context) {
		// Intent intent = new Intent();
		// intent.setClass(context, ErrorActivity.class);
		// context.startActivity(intent);
		// Finish the current activity
		// mDelegate.finish();
	}
}